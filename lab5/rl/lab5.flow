import string;
import math/math;
import ds/tree;

import arith;

main() {
  expr = getUrlParameter("expr");
 
    println("Expression: " + expr);
    e : ArExpr = s2ar(expr);
    println(e);
    println(ar2s(e) + " = " + i2s(ar2i(e)));

    quit(0);
  
}
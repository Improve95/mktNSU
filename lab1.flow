import string;

convert(a: [int])->[string]
{
    if(length(a) == 0) 
        [] 
    else 
        insertArray(convert(tail(a)), 0, i2s(a[0]))
}

main() 
{
    println("Hello world");

    println(convert([1, 2, 3, 4]));

    quit(0);
}

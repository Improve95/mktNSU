import string;
import math/math;
import ds/tree;
import lingo/pegcode/driver;
import fs/filesystem;

export {
    /* lingo */
    infixGrammar = compilePegGrammar("#include labs/block2/arith/infix.lingo");
    postfixGrammar = compilePegGrammar("#include labs/block2/arith/postfix.lingo");

    /* Infix */
    InfixExpr ::= Const, Var, BinOp, Null;
	Const(number : double);
    Var(name : string);
    BinOp(name : string, left: InfixExpr, right: InfixExpr);
    Null();
}
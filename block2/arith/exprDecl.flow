import string;
import math/math;
import ds/tree;
import lingo/pegcode/driver;
import fs/filesystem;

export {
    /* lingo */
    infixGrammar = compilePegGrammar("#include infix.lingo");
    postfixGrammar = compilePegGrammar("#include postfix.lingo");

    /* Infix */
    InfixExpr ::= Const, BinOp;
    BinOp(name : string, left: InfixExpr, right: InfixExpr);
	Const(i : int);

    /* Postfix */
    RpnExpr ::= Const, OpName;
    OpName(op : string);
}
import labs/block2/rational/rational;

main() {
    exprStr = "(1 + 4) / x";
    params = setTreeValues(makeTree(), [Pair("x", RatNum(5, 1))]);

    expRat = s2re(exprStr);
    println(expRat);
    println(re2s(expRat, 0));

    solveRat = calcRe(expRat, params);
    println(reNum2s(solveRat));

    quit(0);
}

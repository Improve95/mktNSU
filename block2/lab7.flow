import labs/block2/arith/postfix;

inputVariablesIntoTree(vars : [string], values : [double]) -> Tree<string, double> {
    foldi(vars, makeTree(), \i, tree, variable -> {
        setTree(tree, variable, values[i])
    });
}

main() {
    vars = ["a", "b"];
    values = [5.0, 4.0];
    expr : string = "(7 + 8) * 5";

    ie = s2ie(expr);
    println(ie);

    varsValues : Tree<string, double> = inputVariablesIntoTree(vars, values);
    // result = calcIe(ie, varsValue);

    // println(result);

    quit(0);
}

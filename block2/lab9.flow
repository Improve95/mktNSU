import labs/block2/rational/polynomRatV2;

main() {
    testPolynom = "x + 0 / x";

    polynomExpr = s2re(testPolynom);

    simpleExpr = simplification(toCommon(polynomExpr));
    println(re2s(simpleExpr, 0));

    vars = [Pair("x", RatNum(2, 1))];
    tree = setTreeValues(TreeEmpty(), vars);

    res = calcRe(simpleExpr, tree);
    println(reNum2s(res));

    quit(0);
}

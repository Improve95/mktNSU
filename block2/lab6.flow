import exprDecl;
import postfix;

main() {
  expr = "2*(5+5)+30";
  parseExpr = parsic(grammar, expr, defaultPegActions);
  println(ex2s(parseExpr, 0));
  println(ex2i(parseExpr));

  println("");

  /* expr2 = "3 4 +";
  parseExpr2 = parseRPN(expr2);
  println(rpnEx2i(parseExpr2)); */

  /* 
    parseRPN(expt)
    translateToInfix(rpn1);
    translateToRPN(infix);

    infixToString(infix) + " = " + d2s(calculateRPN(rpn1, variables))
    RPNToString(rpn2)
   */

  quit(0);
}
